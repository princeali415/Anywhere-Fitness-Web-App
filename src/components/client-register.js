import React from 'react'

export default function ClientRegister(props) {
    console.log(props.newUser)
    return (
        <div>
            <button>Check</button>
            <h1> This is a check to</h1>
        </div>
    )
}
